#include <iostream>
#include <fstream>
#include <string>
#include <iostream>

using namespace std;

int userNumbers(){
    string line;
    ifstream metaFile("./metadata.txt");

    while( getline( metaFile, line, ',' ) ){
        
    }
}